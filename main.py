import telebot
import os

TOKEN = os.getenv('BOT_TOKEN')
bot = telebot.TeleBot(TOKEN)

# আপনার নিজের আইডি এখানে বসান (যাতে অন্য কেউ অ্যাডমিন কমান্ড না চালাতে পারে)
ADMIN_ID = 000000000 # আপনার আইডিটি এখানে দিন

@bot.message_handler(commands=['start'])
def start(message):
    bot.reply_to(message, "🌟 ExpertBrain Pro সচল! \n\nসবগুলো মেনু দেখতে নিচের মেনু বাটনে ক্লিক করুন অথবা টাইপ করুন /help")

@bot.message_handler(commands=['admin'])
def admin_panel(message):
    if message.from_user.id == ADMIN_ID:
        bot.reply_to(message, "👑 স্বাগতম বস! \nবর্তমানে আপনার ইউজার সংখ্যা: ১০৫ জন। \nআজকের আয়: ৫০০ টাকা।")
    else:
        bot.reply_to(message, "❌ এই কমান্ডটি শুধু মালিকের জন্য।")

@bot.message_handler(commands=['help'])
def help(message):
    bot.reply_to(message, "কিভাবে সাহায্য করতে পারি?\n১. প্রশ্ন করতে /ask\n২. আইডি জানতে /id\n৩. সাপোর্ট @YourUsername")

bot.polling(none_stop=True)
